## Проект

Небольшая браузерная игра match-3 на Phaser 3 + TypeScript, собранная на Vite.

## Цель проекта

Сделать базовую playable-версию match-3 без серверной части: с игровым полем, базовой логикой совпадений, подсчётом очков и возможностью перезапуска.

## Стартовое состояние проекта

- Проект находится на этапе стартового прототипа.
- Приложение уже запускает Phaser внутри страницы Vite.
- В `src/main.ts` собрана минимальная оболочка страницы с заголовком, строкой технологий и крупным контейнером под игру.
- В `src/game/config.ts` задан базовый конфиг Phaser: размер 960x540, `FIT`, `CENTER_BOTH`, подключены сцены `BootScene` и `MainMenuScene`.
- `BootScene` сразу переводит приложение на основную игровую сцену.
- Добавлена базовая `GameScene`: сейчас она показывает стартовое поле 8x8, собранное из модели в памяти.
- Добавлена минимальная модель поля и стартовая генерация без готовых матчей.
- Добавлены выбор фишки и swap между соседними клетками с простой анимацией.
- Добавлены поиск совпадений 3+ по горизонтали и вертикали, проверка результата swap и откат невалидного хода.
- Добавлены удаление совпавших фишек и базовое начисление очков.
- Добавлено падение фишек вниз после удаления совпадений.
- Добавлено дозаполнение поля новыми фишками сверху после падения.
- Добавлена обработка каскадов после дозаполнения до полной стабилизации поля.
- Пользовательский ввод блокируется на время swap, анимаций и автоматической обработки хода.
- На сцене отображается текущий счёт и добавлена кнопка рестарта.
- Рестарт выполняется через перезапуск сцены с полным пересозданием поля и сбросом счёта.
- Добавлено управление через drag-and-drop: фишку можно перетаскивать в сторону соседней клетки для обмена.

## Текущая структура проекта

- `src/main.ts` — входная точка приложения, разметка оболочки страницы, создание экземпляра Phaser.
- `src/style.css` — стили общей страницы, верхнего заголовка и расширенного контейнера с игрой.
- `src/game/config.ts` — базовая конфигурация Phaser.
- `src/game/constants.ts` — базовые константы поля 8x8, размеров ячеек и типов фишек.
- `src/game/boardModel.ts` — минимальная модель поля, генерация стартового состояния, удаление матчей, падение и дозаполнение фишек.
- `src/game/game.ts` — фабрика создания `Phaser.Game`.
- `src/game/matchFinder.ts` — поиск совпадений 3+ по горизонтали и вертикали.
- `src/game/types.ts` — базовые типы координат, типов фишек, состояния поля и матч-групп.
- `src/game/scenes/BootScene.ts` — стартовая сцена-переход.
- `src/game/scenes/GameScene.ts` — минимальная игровая сцена, отрисовывающая поле, обрабатывающая выбор, swap, drag-and-drop, удаление матчей, падение, дозаполнение, каскады, счёт и рестарт.
- `src/game/scenes/MainMenuScene.ts` — прежний экран-заглушка, сейчас больше не используется в конфиге сцен.
- `public/` и `src/assets/` — статические ассеты проекта.
- `package.json` — стандартные скрипты `dev`, `build`, `preview`, `lint`, `format`, `format:check`.

## Уже выполненные шаги

- Создан базовый проект на Vite + TypeScript.
- Подключён Phaser 3.
- Настроен запуск игры внутри DOM-контейнера.
- Добавлены базовые сцены `BootScene` и `MainMenuScene`.
- Собран стартовый визуальный экран-прототип вместо пустого шаблона.
- Подготовлена базовая структура папки `src/game/` для дальнейшего развития игровых сцен.
- Добавлена минимальная `GameScene` с тестовой сеткой 8x8.
- Вынесены базовые константы поля и простые типы для дальнейшей логики.
- Добавлена минимальная модель поля 8x8 в памяти.
- Реализована генерация стартового состояния без готовых матчей по горизонтали и вертикали.
- `GameScene` переведена с тестовой сетки на отрисовку стартового набора фишек из модели.
- Добавлены выбор фишки по клику или тапу и визуальное выделение выбранной фишки.
- Реализован swap только между соседними клетками с простой анимацией обмена.
- Реализован поиск совпадений 3+ по горизонтали и вертикали.
- После swap добавлена проверка результата: невалидный ход откатывается, валидный сохраняется и визуально помечается как матч.
- Добавлено удаление совпавших фишек из модели и со сцены.
- Добавлен базовый счёт: по 10 очков за каждую удалённую фишку.
- Реализовано падение оставшихся фишек вниз внутри каждого столбца после удаления совпадений.
- Модель поля и визуальное состояние сцены теперь синхронно обновляются после падения.
- Реализовано дозаполнение пустых клеток новыми фишками сверху после падения.
- Новые фишки создаются выше поля и анимированно опускаются в нужные позиции.
- Добавлен цикл стабилизации поля: после дозаполнения игра повторно ищет матчи и продолжает обработку каскадов, пока совпадения не закончатся.
- Пользовательский ввод теперь блокируется на всё время обработки хода и разблокируется только после завершения каскадов или отката невалидного swap.
- На сцене закреплён HUD минимального объёма: текущий счёт и кнопка рестарта без вынесения в отдельный модуль.
- Для сброса игры используется перезапуск `GameScene`, а стартовые данные заново инициализируются в `create`.
- Добавлен drag-and-drop без отдельной подсистемы жестов: направление обмена определяется по доминирующему смещению dragged-фишки.
- Обновлена оболочка страницы: описание удалено, строка технологий вынесена над игрой, а игровой экран сделан основным визуальным фокусом.

## Важные замечания

- Настройки ESLint и Prettier были вручную изменены после первоначальной генерации проекта, поэтому их нельзя считать стандартными шаблонными.
- При дальнейшей работе не менять конфиги без отдельной необходимости и явного понимания причины.
- Текущее содержимое `eslint.config.js` выглядит как ранее вручную адаптированный конфиг и не похоже на исходный минимальный конфиг Vite для этого проекта.
- Игра будет реализовываться пошагово, без попытки собрать всю механику за один крупный этап.
- Swap считается валидным только в том случае, если после него на поле появляется хотя бы один матч.
- Стартовое поле должно генерироваться без готовых матчей.
- На старте используем минимальную архитектуру без лишнего дробления на модули: сначала несколько простых файлов с понятной ответственностью, без преждевременного усложнения.
- Обработка каскадов добавлена прямо в `GameScene` через простой цикл стабилизации поля, без введения отдельной state machine.
- Для рестарта выбран перезапуск сцены как самый короткий и надёжный способ сбросить визуальное и игровое состояние синхронно.
- Drag-and-drop встроен в существующий `GameScene` и использует уже готовую анимацию swap, чтобы не дублировать логику обмена.
- Для внешнего layout сохранён минимальный подход: без отдельного UI-фреймворка и без дополнительной структуры поверх текущей страницы.

## Согласованный пошаговый план реализации

1. Добавить отдельную игровую сцену вместо текущего стартового экрана-заглушки.
2. Зафиксировать базовые константы и типы: поле 8x8, размеры ячеек, набор типов фишек, базовые структуры данных.
3. Реализовать минимальную модель поля и генерацию стартового состояния без готовых матчей.
4. Отрисовать поле и фишки в `Phaser`, связав визуальное представление с моделью.
5. Добавить выбор фишек и swap только между соседними клетками.
6. Реализовать проверку swap: если матч не создан, обмен откатывается как невалидный.
7. Реализовать поиск совпадений 3+ по горизонтали и вертикали.
8. Добавить удаление совпавших фишек и начисление очков.
9. Реализовать падение фишек вниз в освободившиеся ячейки.
10. Реализовать дозаполнение поля новыми фишками сверху.
11. Добавить обработку каскадов: повторение цикла матчей, удаления, падения и дозаполнения до стабилизации поля.
12. Добавить HUD: отображение счёта и кнопку рестарта.
13. Провести ручную проверку полного базового цикла игры.

Статус по этапам:

- Этап 1 выполнен: `GameScene`, базовые константы, базовые типы и тестовая сетка на экране уже добавлены.
- Этап 2 выполнен: минимальная модель поля и стартовая генерация без готовых матчей уже добавлены.
- Этап 3 выполнен: выбор фишек и swap соседних клеток уже добавлены без проверки матчей.
- Этап 4 выполнен: проверка swap на создание матча и откат невалидного хода уже добавлены.
- Этап 5 выполнен: удаление совпавших фишек и базовое начисление очков уже добавлены.
- Этап 6 выполнен: падение фишек вниз в пустые клетки уже добавлено без дозаполнения и без каскадов.
- Этап 7 выполнен: дозаполнение поля новыми фишками сверху уже добавлено.
- Этап 8 выполнен: каскады после дозаполнения и блокировка ввода на время обработки хода уже добавлены.
- Этап 9 выполнен: текущий счёт, кнопка рестарта и корректный сброс состояния игры уже добавлены.
- Этап 10 выполнен: drag-and-drop для обмена соседних фишек уже добавлен.
- Этап 11 выполнен: игровой экран расширен, описание убрано, технологии перенесены над игрой.
- Следующий шаг: ручная проверка полного базового цикла игры и обоих способов управления.

## Предполагаемые стартовые файлы для первого этапа

- `src/game/scenes/GameScene.ts` — основная игровая сцена для первого рабочего прототипа поля.
- `src/game/constants.ts` — константы поля, размеров и базовых параметров игры.
- `src/game/types.ts` — простые типы для координат, фишек и состояния поля.
- `src/game/boardModel.ts` — минимальная модель поля, генерация стартового состояния и базовые операции над сеткой.
- `src/game/matchFinder.ts` — поиск совпадений 3+ без лишнего усложнения.
- `src/game/ui/Hud.ts` или временно логика HUD внутри `GameScene` — в зависимости от объёма первого этапа.
- `src/game/config.ts` — подключение новой игровой сцены, когда начнётся реализация.

На первом этапе лучше начать с минимального набора:

- `src/game/scenes/GameScene.ts`
- `src/game/constants.ts`
- `src/game/types.ts`
- `src/game/boardModel.ts`
- `src/game/matchFinder.ts`
